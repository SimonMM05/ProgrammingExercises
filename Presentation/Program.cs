// Deklaration og initialisering af en tom array med fem elementer
int[] numbers = new int[5];

// Deklaration og initialisering af en array med værdier
int[] numbers1 = new int[] { 1, 2, 3, 4, 5 };

int[] numb = { 1, 2, 3, 4, 5 };
int thirdNum = numbers[2]; 

Console.WriteLine(thirdNum); // thirdNumber vil være 3
// fordi det er det tredje element i arrayet som har index 2

int[] num = { 1, 2, 3, 4, 5 };
int length = num.Length; // længden af arrayet vil være 5

int[] numbersss = { 1, 2, 3, 4, 5 };
for (int i = 0; i < numbersss.Length; i++)
{
    Console.WriteLine(numbersss[i]);
}
// Brug af et for loop til at udskrive alle elementer i arrayet

int[] numberr = { 1, 2, 3, 4, 5 };

foreach (int numberrr in numberr)
{
    Console.WriteLine(numberrr);
}

